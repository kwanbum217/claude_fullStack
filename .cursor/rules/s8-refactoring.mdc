---
description: 코드 리팩토링 스킬 (S8). 코드 구조 개선, 외부 CSS 분리, 최적화 요청 시 발동됩니다.
globs: workspace/**/*
alwaysApply: false
---

# S8. 코드 리팩토링 스킬

기존 코드의 품질과 구조를 개선합니다.

## 리팩토링 수칙
- 인라인 CSS -> 외부 CSS 분리
- 반복 코드 제거, 시맨틱 태그 적용
- 변경 전후 비교(diff) 제공 및 승인 후 수정
- 기능 변경 없이 구조만 개선
