# AGENTS.md — AI 에이전트 협업 가이드

> 이 프로젝트에 접속하는 모든 AI 에이전트(Claude, Codex, Cursor, Antigravity, OpenCode 등)는
> 이 파일을 먼저 읽고 프로젝트 컨텍스트를 파악한 뒤 작업을 수행해야 합니다.

---

## 1. 프로젝트 개요

| 항목       | 내용                                                    |
| ---------- | ------------------------------------------------------- |
| 프로젝트명 | **claude_fullStack** — 풀스택 웹 개발 학습 프로젝트     |
| 학습 과정  | HTML → CSS → JavaScript → Node.js → React → DB(MongoDB/MySQL) |
| 현재 진도  | **8회차** (HTML/CSS/JS 완료, Git & GitHub 기초 학습 중) |
| 학습 방식  | 오프라인 수업 → 메모 정리 → 실습 코드 작성              |

---

## 2. 프로젝트 디렉토리 구조

```
claude_fullStack/
├── AGENTS.md            # (이 파일) AI 에이전트 협업 가이드
├── SKILLS.md            # 에이전트 역할·스킬 정의
├── memo/                # 수업 진도 메모 (일차별 학습 노트)
│   ├── ot.txt           # OT — 과정 소개, 개발환경 세팅
│   ├── day01.txt        # 1회차 — 웹 개요, HTML 기초, 태그
│   ├── day02.txt        # 2회차 — 리스트, 테이블, 폼, 시맨틱 태그
│   ├── day03.txt        # 3회차 — CSS 소개, 텍스트 스타일링, 색상
│   ├── day04.txt        # 4회차 — CSS 적용 방법, 선택자, 박스 모델
│   ├── day05.txt        # 5회차 — CSS 응용, 레이아웃
│   ├── day06.txt        # 6회차 — DOM 조작, fetch, HTTP/API, 브라우저-서버-DB 흐름
│   ├── day07.txt        # 7회차 — JSON 이해, 기능 명세 정리, 미니 게시판 CRUD 실습
│   └── day08.txt        # 8회차 — Git 개념, git init/status/add/commit, GitHub 연동
├── resource/            # 학습 리소스 (이미지, 참고 자료 등)
└── workspace/           # 실습 코드
    ├── login1.html          # 로그인 폼 실습
    ├── index3.html          # 시맨틱 HTML 레이아웃 실습
    ├── day03/               # 3회차 실습
    │   ├── page1.html       # 외부 CSS 연결 실습 1
    │   ├── page2 .html      # 외부 CSS 연결 실습 2
    │   ├── profile.html     # 프로필 페이지 (내부 CSS)
    │   ├── standard_html.html  # HTML 요소 종합 레퍼런스
    │   ├── style1.css       # 외부 CSS 파일
    │   └── css/
    │       └── standard_html.css  # 종합 레퍼런스용 CSS
    ├── day04/               # 4회차 실습
    │   ├── box_practics.html       # 박스 모델 실습
    │   ├── div_practics.html       # display 속성 실습
    │   └── selector_practics.html  # CSS 선택자 실습
    ├── day05/               # 5회차 실습
    ├── day06/               # 6회차 실습
    ├── day07/               # 7회차 실습 (미니 게시판 index1.html, style1.css, script1.js)
    └── day08/               # 8회차 실습 (git_practics)
```

---

## 3. 접속 에이전트 목록 및 역할

이 프로젝트에는 다음 AI 에이전트들이 접속하여 협업합니다.

### 3.1 Claude (Anthropic)
| 항목       | 내용                                                |
| ---------- | --------------------------------------------------- |
| 설정 파일  | `CLAUDE.md` (필요 시 생성)                          |
| 주요 역할  | 코드 리뷰, 학습 질의응답, 코드 생성, 디버깅          |
| 강점       | 긴 컨텍스트 이해, 한국어 설명, 복잡한 로직 분석      |
| 참고       | `.claude/` 디렉토리에 프로젝트별 설정 가능           |

### 3.2 Codex (OpenAI)
| 항목       | 내용                                                |
| ---------- | --------------------------------------------------- |
| 설정 파일  | `codex.md` (필요 시 생성)                           |
| 주요 역할  | 코드 자동 완성, 코드 변환, 반복 작업 자동화          |
| 강점       | 빠른 코드 생성, 다양한 언어 지원                     |
| 참고       | 샌드박스 환경에서 실행, `AGENTS.md` 자동 인식        |

### 3.3 Cursor
| 항목       | 내용                                                |
| ---------- | --------------------------------------------------- |
| 설정 파일  | `.cursorrules` (필요 시 생성)                       |
| 주요 역할  | IDE 내 코드 편집, 인라인 코드 생성, 리팩토링         |
| 강점       | 실시간 코드 편집, 파일 컨텍스트 자동 인식             |
| 참고       | `.cursor/` 디렉토리에 프로젝트 설정 저장 가능        |

### 3.4 Antigravity (Google DeepMind)
| 항목       | 내용                                                |
| ---------- | --------------------------------------------------- |
| 설정 파일  | `.gemini/` 디렉토리 활용                            |
| 주요 역할  | 프로젝트 분석, 파일 생성·편집, 멀티 에이전트 협업     |
| 강점       | 서브 에이전트 활용, 병렬 작업, 이미지 생성            |
| 참고       | `SKILLS.md`, `AGENTS.md` 자동 인식                  |

### 3.5 OpenCode
| 항목       | 내용                                                |
| ---------- | --------------------------------------------------- |
| 설정 파일  | `.opencode/` (필요 시 생성)                         |
| 주요 역할  | 터미널 기반 코드 편집, CLI 작업 지원                 |
| 강점       | 경량 터미널 환경, 빠른 파일 조작                     |
| 참고       | `AGENTS.md` 자동 인식                               |

---

## 4. 에이전트 공통 규칙

### 4.1 언어 및 소통
- **한국어**를 기본 소통 언어로 사용합니다.
- 코드 주석도 한국어로 작성합니다.
- 기술 용어는 영어 원문을 병기합니다. (예: 선택자(Selector))

### 4.2 파일 작업 규칙
- `memo/` 폴더: 수업 메모 파일입니다. **기존 내용을 임의로 수정하지 마세요.**
  - 새 내용 추가 시 기존 형식(들여쓰기, 번호 체계)을 따릅니다.
- `workspace/` 폴더: 실습 코드 폴더입니다. 
  - 일차별 하위 폴더(`day01/`, `day02/`, …)를 만들어 관리합니다.
  - 기존 실습 코드를 수정할 때는 반드시 사용자 확인을 받습니다.
- `resource/` 폴더: 학습 리소스(이미지, 참고 자료)를 저장합니다.

### 4.3 코딩 컨벤션
- HTML 파일은 `<!DOCTYPE html>` 선언으로 시작합니다.
- 들여쓰기는 **공백 2칸(2 spaces)** 을 사용합니다 (기존 코드 스타일 준수).
- CSS 파일은 가능한 외부 스타일시트로 분리합니다.
- 파일명은 **영어 소문자 + 언더스코어(_)** 를 사용합니다.
- `<meta charset="UTF-8">` 을 반드시 포함합니다.
- **Claude 디자인 시스템(S9) 준수**: HTML/CSS 생성 시 DESIGN.md 기반 토큰(코랄/크림 색상, Garamond/Inter 폰트 스택, 뱃지, 카드 레이아웃)을 기본 디자인 규격으로 적용합니다.
- **이모지(Emoji) 사용 금지**: 코드, 주석, 문서 등 모든 산출물에서 이모지를 사용하지 않습니다.

### 4.4 학습 프로젝트 주의사항
- 이 프로젝트는 **학습 목적**입니다. 복잡한 프레임워크 도입을 지양합니다.
- 현재 진도에 맞는 기술만 사용합니다 (현재: HTML + CSS + JavaScript 기초/DOM/JSON/CRUD + Git & GitHub).
- 코드 설명 시 **원리와 동작 방식**을 함께 설명합니다.
- 정답을 바로 주기보다 **학습에 도움이 되는 힌트**를 우선 제공합니다.

---

## 5. 학습 진도 요약

| 회차   | 주제                          | 핵심 개념                                            |
| ------ | ----------------------------- | ---------------------------------------------------- |
| OT     | 과정 소개                     | 풀스택 커리큘럼, 개발환경 세팅 (VS Code, Chrome, Node.js) |
| Day 01 | 웹 개요 + HTML 기초           | HTTP, 클라이언트/서버, HTML 구조, 기본 태그            |
| Day 02 | HTML 심화                     | 리스트, 테이블, 폼, 시맨틱 태그, 블록/인라인 요소     |
| Day 03 | CSS 소개                      | CSS 문법, 텍스트 스타일링, 색상 표현                   |
| Day 04 | CSS 적용·선택자·박스 모델     | 외부/내부/인라인 CSS, 선택자 4종, 박스 모델, display   |
| Day 05 | CSS 응용·레이아웃             | CSS 응용 및 배치                                      |
| Day 06 | JS 기초·DOM 조작·fetch·API    | DOM 조작, fetch 비동기 통신, HTTP/API, 브라우저-서버-DB 흐름 |
| Day 07 | JS 미니 게시판 CRUD·JSON      | JSON 개념, 기능 명세 프롬프트, 목록 출력, 게시글 추가/삭제, 부분 수정 |
| Day 08 | Git & GitHub 기초             | Git 개념, git init/status/add/commit, GitHub 연동    |
| Day 09 | *(진행 예정)*                 | —                                                    |

---

## 6. 에이전트 간 충돌 방지

- 동시에 같은 파일을 편집하지 않습니다.
- 파일 생성/수정 전 현재 프로젝트 상태를 먼저 확인합니다.
- 대규모 변경 시 사용자 승인을 받습니다.
- 이 `AGENTS.md` 파일을 수정할 때는 반드시 사용자에게 알립니다.

---

## 7. 향후 확장 계획

학습 진도에 따라 아래 기술이 순차적으로 추가될 예정입니다:

```
현재 ──────────────────────────────────────────── 향후
HTML [완료] → CSS [완료] → JavaScript [진행중] → Node.js → React → DB
```

새로운 기술 스택 도입 시 이 문서와 `SKILLS.md`를 함께 업데이트합니다.
